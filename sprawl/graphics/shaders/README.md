# Shaders
Shader files should be named according to [`glslang`](https://www.khronos.org/opengles/sdk/tools/Reference-Compiler/). Compiled shader modules will be output with the same name as their source, but with an added `.spv` extension. 
