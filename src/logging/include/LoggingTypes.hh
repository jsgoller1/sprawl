enum LoggingSubsystem {
  NONE,
  COLLISIONS,
  INPUT,
  LOGGING,
  MATH,
  PHYSICS,
  RENDERING,
  SDL,
  WADLOADER,
  WORLD,
  ZONE,
};

enum LogLevel { FATAL = 4, ERROR = 3, WARN = 2, INFO = 1, DEBUG = 0 };
